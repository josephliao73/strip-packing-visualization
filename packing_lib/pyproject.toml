[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "packing_lib"
version = "0.1.0"
description = "Helper library for bin packing algorithms"
requires-python = ">=3.8"

[project.urls]
Homepage = "https://github.com/yourusername/packing"
